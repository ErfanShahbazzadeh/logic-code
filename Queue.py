class queuecalss:
    def __init__(self,size):
        front = 0
        rear = 0
        max_size = self.size
        Queue = list()

    def Enqueue(self,object):
        if IsEmpty() == True:
            self.Queue[self.rear]  = object

    def Dequeue(self):
        if IsFull() == True:
            return object
    
    def Peek(self):
        return object

    def ReverseQueue():
        return queue

    def IsEmpty(): 
        return boolean

    def IsFull(): 
        return boolean

obj = queuecalss()